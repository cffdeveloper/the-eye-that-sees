/**
 * Legacy monthly plan (Paystack subscription path still honored for existing rows).
 * New purchases use token credits; see `creditsConfig` for MIN_CREDIT_PURCHASE_USD.
 */
export const SUBSCRIPTION_USD_MONTHLY = 30;
